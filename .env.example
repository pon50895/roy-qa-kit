# 複製成 .env.<env>（如 .env.uat / .env.prod），勿 commit
ENV=uat
BASE_API=https://api.example.com
BASE_WEB=https://web.example.com
ADMIN_USER=
ADMIN_PASS=
API_TOKEN=                 # 由 npm run login 取得（若有 captcha 為人工關卡）
# 環境特性旗標（依專案）
OTP_BYPASS=false
CAPTCHA=true
